# Railway RUNTIME bağımlılıkları (web + worker) — SLIM.
# Canlı yol (app_unified/app_pro/app_trader/paper_engine/worker/auto_*) sadece
# bunları import eder. lightgbm/scipy/scikit-learn/playwright RUNTIME'da
# kullanılmaz (yalnız yerel model eğitimi/backtest/scrape için → requirements.txt).

# Liste, start.py'den başlayan GERÇEK import kapanışına göre tutulur
# (25.09.2026'da ölçüldü: 63 yerel modül, 8 dış paket). Haftalık Bakım
# sayfası bu listeyi kapanışla her hafta karşılaştırır.
#
# 25.09.2026 çıkarıldı — canlı yolda hiç import edilmiyordu:
#   plotly      app_v2 grafik için kütüphane kullanmıyor (kodda gerekçesi yazılı);
#               yalnız eski app.py/app_pro/app_trader'da vardı
#   requests    yalnız 02_VERI/scrapers/football_data_downloader.py (yerel betik)
#   sqlalchemy  projede HİÇBİR yerde import edilmiyor (create_engine de yok)
# Üçü de requirements.txt'te (yerel/geliştirme) duruyor.

streamlit==1.57.0
pandas==2.2.3
numpy==2.2.1
psycopg2-binary==2.9.9
APScheduler==3.11.2

# Vibe Betting sohbet asistanı (09_TOTO/vibe_*) — web sürecinde çalışır, RUNTIME.
# Anahtar (ANTHROPIC_API_KEY_BET_AGENTS) yoksa hiç çağrılmaz; sayfa bunu söyler.
anthropic>=0.94,<2
